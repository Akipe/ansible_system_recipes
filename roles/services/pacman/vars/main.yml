---

pacman_app_archlinux_common:
  - xdelta3
  - pacman-contrib
  - pacman-mirrorlist
  - perl-locale-gettext

pacman_app_archlinux:
  - reflector

pacman:
  reflector:
    command: reflector --country France --latest 200 --age 24 --protocol https --sort rate --save /etc/pacman.d/mirrorlist
