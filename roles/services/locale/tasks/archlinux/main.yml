---

- name: Load tasks for terminal
  include_tasks: "{{ item }}.yml"
  loop:
    - 'timesyncd'
    - 'timezone'
    - 'keyboard-cli'
    - 'language'

- name: Load tasks for gui
  include_tasks: "{{ item }}.yml"
  loop:
    - 'keyboard-gui'
    - 'dictionary'
  when: locale_is_gui
