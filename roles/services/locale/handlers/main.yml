---
- name: Execute localectl for update keymap
  command: localectl set-keymap --no-convert fr

- name: Update initramfs
  command: mkinitcpio -P

- name: Set immediately language
  command: "export LANG={{ role_language_default }}"
