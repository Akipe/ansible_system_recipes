---
- name: Install Yay
  become: yes
  become_user: ansible_aur
  aur:
    use: makepkg
    state: present
    name:
      - yay

- name: Remove old makepkg directory
  file:
    path: /tmp/makepkg/
    state: absent
