---

# Wait for Ansible v2.8 for snap
#- name: Install livepatch
#  snap:
#    name: canonical-livepatch
#    state: present
#  register: livepatch_installed

#- name: Active livepatch
#  ansible.builtin.command: "canonical-livepatch enable {{ canonical_livepatch_token }}"
#  when: livepatch_installed.changed

# todo: add "canonical-livepatch status --verbose" and add "/snap/bin" to PATH