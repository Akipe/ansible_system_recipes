---

- name: Load tasks for Fedora
  include_tasks: fedora/main.yml
  when: ansible_distribution == "Fedora"

- name: Load tasks for Archlinux
  include_tasks: archlinux/main.yml
  when: ansible_distribution == "Archlinux"

- name: Enable systemd unit pcscd
  become: true
  systemd:
    name: pcscd.socket
    enabled: true
    state: started

- name: Add script for rotate with several Yubikey key for SSH
  template:
    src: yk_rotate.bash.j2
    dest: "/home/{{ item.name }}/.local/bin/yk_rotate.bash"
    owner: "{{ item.name }}"
    group: "{{ item.name }}"
    mode: u+rwx,g+rx,o+rx
  loop: "{{ role_user_listUsers }}"