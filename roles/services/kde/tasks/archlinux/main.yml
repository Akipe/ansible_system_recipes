---

- name: Install KDE and base apps
  pacman:
    state: present
    name: "{{ kde_appToInstall_base_archlinux }}"

# - name: Install KDE apps (AUR)
#   become: yes
#   become_user: ansible_aur
#   aur:
#     use: yay
#     state: present
#     aur_only: yes
#     name: "{{ kde_appToInstall_base_archlinux_aur }}"

# - name: Install Thunar
#   pacman:
#     state: present
#     name: "{{ kde_appToInstall_thunar_archlinux }}"

- name: Install Dolphin
  pacman:
    state: present
    name: "{{ kde_appToInstall_dolphin_archlinux }}"

# - name: Install Dolphin (AUR)
#   become: yes
#   become_user: ansible_aur
#   aur:
#     use: yay
#     state: present
#     aur_only: yes
#     name:
#       - resvg
#       - kde-thumbnailer-apk
#       - kde-thumbnailer-blender
#       - kde-thumbnailer-epub

- name: Install apps utils for theming
  pacman:
    state: present
    name: "{{ kde_app_utils_theme_archlinux }}"

- name: Install themes Materia
  pacman:
    state: present
    name: "{{ kde_app_theme_materia_archlinux }}"
