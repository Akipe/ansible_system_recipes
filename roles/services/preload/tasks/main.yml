---

- name: Installation
  become: yes
  become_user: ansible_aur
  aur:
    use: yay
    state: present
    aur_only: yes
    name: preload

- name: Enable and start Preload service
  systemd:
    name: preload.service
    daemon_reload: yes
    enabled: yes
    state: started
