---
- name: Install usefull app for guest Qemu
  apt:
    name: "{{ qemuGuest_appToInstall_debian }}"
    state: present

- name: Enable QEMU agent
  service:
    name: qemu-guest-agent
    state: started
    enabled: yes
