---
- name: Install basics plugins
  dnf:
    state: present
    name: "{{ dnf_appToInstall_fedora }}"

- name: Add configuration
  template:
    src: dnf.conf.j2
    dest: /etc/dnf/dnf.conf
    owner: root
    group: root
    mode: u+rw,g+r,o+r
