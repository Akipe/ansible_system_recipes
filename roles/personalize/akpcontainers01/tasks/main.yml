---
- name: Configure 10Gbit Network interface
  template:
    src: systemd_network/ens19-10gbnet.network.j2
    dest: /etc/systemd/network/ens19-10gbnet.network
    owner: root
    group: root
    mode: 0644
  notify: Restart network service

# TODO: move this tasks to allow "Restart network service" before launch it
# - name: Configure akpServer01 iSCSI containers connexion
#   open_iscsi:
#     show_nodes: yes
#     discover: yes
#     portal: 
#   register: iscsi_debug

- name: Load Docker rules
  include_tasks: docker.yml
